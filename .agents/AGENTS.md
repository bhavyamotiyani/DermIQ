# Custom Rules for Skincare Project

## Environment and Command Execution
- The agent is pre-approved and allowed to run Python and package manager commands using the virtual environment (`.venv`) without requesting confirmation, to the maximum extent permitted by the system sandboxing constraints.
